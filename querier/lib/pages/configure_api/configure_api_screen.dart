import 'package:flutter/material.dart';

class ConfigureAPIScreen extends StatelessWidget {
  const ConfigureAPIScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Configure API'),
      ),
      body: const Center(
        child: Text('Configuration screen coming soon...'),
      ),
    );
  }
}
